# Exploring NYC Public School Test Scores
### A bonus project in DataCamp's Career Track: Associate Data Scientist in Python. Uses Pandas for data manipulation.

### This projects uses `pandas` to filter and aggregate the data. The code is **completely my own work**, but the datasets, pictures, and the first code block belongs to DataCamp. 

---
 I learned so much about the syntaxes, the techniques, and the parameters in functions like `pd.groupby` and `pd.round`. It also enhanced my understanding and application of `.loc/.iloc` in higher-level filtering of datasets.
